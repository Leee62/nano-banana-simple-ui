# Copy this file to `.env.production` before deploying.
GEMINI_API_KEY=replace_with_your_real_production_key
PORT=8787

# Public runtime config injected into the final client bundle at request time.
PUBLIC_APP_NAME=Nano Banana UI
PUBLIC_API_BASE_URL=/api
PUBLIC_DEFAULT_LOCALE=zh
